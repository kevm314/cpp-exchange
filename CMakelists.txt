cmake_minimum_required(VERSION 3.1...3.25)
set(CMAKE__CXX_STANDARD 20)
set(CMAKE_CXX_STANDARD_REQUIRED true)
set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -Wall -O0 -fPIC") # -fsanitize=leak -g
option(TEST "enable test build" OFF)


project(
  ExchangeSystem
  VERSION 1.0
  LANGUAGES CXX)

# Find packages here

include_directories("/usr/local/lib")
include_directories(matchmaker)

add_subdirectory(./matchmaker)


add_executable(exchange main.cpp)
target_link_libraries(exchange matchmaker)